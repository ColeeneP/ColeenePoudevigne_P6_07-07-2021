le dossier back contient normalement un fichier .env contenant deux lignes :

DB_HOST = VOTRE_CONNEXION_A_MONGODB
TOKEN = VOTRE_TOKEN

Pour des raisons de sécurité ce fichier n'est pas disponible dans le repo.